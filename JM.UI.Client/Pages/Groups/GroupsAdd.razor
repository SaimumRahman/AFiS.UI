@page "/GroupsAdd"
@page "/GroupsAdd/{Id:int}"
@using JM.UI.Entities.Model.Groups
@attribute [Authorize]
@inherits GroupsAddComponent

<PageTitle>@PageTitle</PageTitle>

<RadzenContent Container="main" class="p-4">
    <RadzenCard class="mb-4">
        <RadzenRow AlignItems="AlignItems.Center">
            <RadzenColumn Size="12">
                <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.5rem">
                    <RadzenIcon Icon="group" Style="font-size: 2rem; color: var(--rz-primary);" />
                    <RadzenText TextStyle="TextStyle.H4" class="mb-0">@PageTitle</RadzenText>
                </RadzenStack>
            </RadzenColumn>
        </RadzenRow>
    </RadzenCard>

    <RadzenTemplateForm TItem="GroupModelDTO" Data="@Group" Submit="@Save">
        @if (IsLoading)
        {
            <RadzenProgressBar Value="100" ShowValue="false" Mode="ProgressBarMode.Indeterminate" />
        }
        else
        {
            <RadzenCard>
                <RadzenStack Gap="1rem">
                    
                    <RadzenStack Gap="0.5rem">
                        <RadzenLabel Text="Name" Component="Name" />
                        <RadzenTextBox Name="Name" @bind-Value="@Group.Name" class="w-100" />
                        <RadzenRequiredValidator Component="Name" Text="Name is required" />
                    </RadzenStack>

                    <RadzenRow JustifyContent="JustifyContent.End" Gap="1rem" class="mt-4">
                        <RadzenButton Text="Cancel" ButtonStyle="ButtonStyle.Light" Click="@Cancel" Icon="cancel" Disabled="@IsProcessing" />
                        <RadzenButton ButtonType="ButtonType.Submit" Text="@(IsEditMode ? "Update" : "Create")" ButtonStyle="ButtonStyle.Primary" Icon="save" IsBusy="@IsProcessing" />
                    </RadzenRow>
                </RadzenStack>
            </RadzenCard>
        }
    </RadzenTemplateForm>
</RadzenContent>
